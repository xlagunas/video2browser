'use strict';

angular.module('video2browserApp')
  .controller('LoginCtrl', function ($scope, $log, $http, $state) {
    $http.defaults.use
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];

  });
